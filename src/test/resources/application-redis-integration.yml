spring:
  session:
    store-type: redis
  data:
    redis:
      host: localhost
proxy:
  store-mode: Redis
  authentication: simple
  container-backend: docker
  realm-id: my-realm


  users: [ ]

  specs: [ ]
